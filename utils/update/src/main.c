#include "stdio.h"
#include "install.h"

int main(){
   	int arg_count = 0;
    	//unsigned short* arg_list = CommandLineToArgvW(GetCommandLineW(), &arg_count);

	ax_setup_i(NULL, 0);

	while(1);	
    	return 0;
}
